{
  "name": "multiplicato-game",
  "version": "1.0.0",
  "description": "A digital version of the Multiplicato math game.",
  "scripts": {
    "build": "esbuild index.tsx --bundle --minify --outfile=dist/bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
